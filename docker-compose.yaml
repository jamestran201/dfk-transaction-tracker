version: "3.9"
services:

  db:
    image: postgres:12.7
    ports:
      - "5432:5432"
    volumes:
      - app-db-data:/var/lib/postgresql/data
    env_file:
      - .env

  redis:
    image: redis:6.2
    ports:
      - "6379:6379"

volumes:
  app-db-data:
